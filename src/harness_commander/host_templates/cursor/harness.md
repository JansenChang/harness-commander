# Harness

Run the local `harness` CLI with the provided arguments.

## Usage

Pass the exact command arguments you want after `harness`.

Examples:
- `init -p .`
- `distill docs/spec.md --json`
- `install-provider --provider claude`

## Execute

Run:

```bash
harness {{args}}
```
