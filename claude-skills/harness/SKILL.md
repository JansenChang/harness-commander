---
name: harness
description: Run the local harness CLI with the provided arguments
disable-model-invocation: true
allowed-tools: Bash(harness *)
argument-hint: [args...]
---

Run the local `harness` command with the provided arguments:

!`harness $ARGUMENTS`
