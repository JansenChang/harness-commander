# `harness check` 产品定义

## 解决的问题

- 项目变更完成后，缺少统一的质量、安全、团队信仰和产品一致性审计入口。
- 规则文档可能仍是说明型模板，需要明确标记未量化规则。

## 具体作用

- 根据质量、安全、设计信仰和产品约束输出审计结果。
- 默认检查计划文件与生成文档。
- 对规则来源、严重级别、文件位置和处理建议进行结构化展示。

## 当前实现

- 当前默认不依赖宿主模型。
- 未来只允许宿主模型辅助生成审计摘要和处理建议。

## 产物

- 文本审计结果与 JSON 结构化结果。
