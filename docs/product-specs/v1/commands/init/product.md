# `harness init` 产品定义

## 解决的问题

- 项目缺少统一治理目录、基础模板和参考材料，导致后续命令无稳定运行基线。

## 具体作用

- 补齐 Harness-Commander 所需的目录和模板。
- 支持 `-p/--root` 指定目标项目根目录。
- 目录不存在时创建，文件不存在时创建，已存在时跳过。

## 当前实现

- 已实现。
- 不接宿主模型。

## 产物

- 白名单内的治理文件、`docs/` 目录结构和初始化模板。
