# `harness sync` 产品定义

## 解决的问题

- 重大变更发生后 AI 参考材料未同步。
- 团队无法快速判断应更新哪些参考产物。

## 具体作用

- 识别重大变更是否发生。
- 判断受影响的生成类文档和参考类文档。
- 只更新受影响的产物并输出变更摘要。

## 当前实现

- 当前默认由 Harness 主导识别范围、目标产物和内容生成。
- 未来只允许宿主模型辅助摘要或文案增强。

## 产物

- `docs/generated/` 或 `docs/references/` 下的受影响文件。
