# `harness plan-check` 产品定义

## 解决的问题

- 计划文件结构不完整时，AI 仍可能继续执行，造成返工。
- 缺少稳定的计划合规检查入口。

## 具体作用

- 检查计划文件是否满足结构要求。
- 校验治理文档引用完整性。
- 输出问题列表与修复建议。

## 当前实现

- 当前以规则校验为主。
- 默认不依赖宿主模型；未来只允许摘要或修复建议增强。

## 产物

- 文本校验结果与 JSON 结构化结果。
