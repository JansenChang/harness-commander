# `harness collect-evidence` 产品定义

## 解决的问题

- 宿主工具在任务执行过程中缺少统一的证据留痕入口。
- 成功与失败记录分散，难以回溯具体执行命令、退出码和关键日志。

## 具体作用

- 在任务关键节点记录命令执行证据。
- 保留成功和失败两类执行记录。
- 统一沉淀时间范围、退出码、命令、摘要、关键日志片段和产物路径。

## 当前实现

- 默认不依赖宿主模型。
- 未来最多允许补充更易读摘要，但不得改写原始事实。

## 产物

- `docs/generated/evidence/` 下的证据记录文件。
